#include "header.h"

int main(int argc, char const *argv[]) {
    ifstream fin("data.in");
    ifstream fout("data.out");

    cout<<"Hello world!\n";

    fout.close();
    fin.close();
    return 0;
}
